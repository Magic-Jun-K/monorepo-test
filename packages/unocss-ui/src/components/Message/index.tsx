/**
 * @description 消息提示组件
 */
import { useMessage } from './useMessage';

const message = { useMessage };

export { message };
export type { MessageConfig, MessageConfigBase, MessageInstance } from './types';
