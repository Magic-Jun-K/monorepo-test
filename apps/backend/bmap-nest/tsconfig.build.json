{
  "extends": "./tsconfig.json",
  "exclude": ["node_modules", "test", "dist", "**/*spec.ts", "scripts/**/*"],
  "compilerOptions": {
    "incremental": false, // 不生成tsconfig.build.tsbuildinfo,
    "noEmit": false
  }
}
